# Reservado para credenciais futuras (integração Open Finance via Pluggy — Fase 7).
# Nenhuma variável é necessária ainda. Copie para .env quando essa fase chegar
# e preencha com valores reais — .env nunca é versionado (ver .gitignore).
